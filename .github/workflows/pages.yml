name: Deploy live demo to GitHub Pages

# Builds the Fluid.Avalonia.Acrylic.Demo.Browser WebAssembly head and publishes it to
# GitHub Pages. Enable once in the repo:
#   Settings → Pages → Build and deployment → Source = "GitHub Actions".

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, and let an in-progress production deploy COMPLETE rather than
# cancelling it. A burst of pushes to main (e.g. a demo push immediately followed by a release) would
# otherwise cancel one deploy mid-run and leave the Pages backend in a state where the next one fails
# outright ("Deployment failed, try again later"). Runs still queue so the newest content wins; only
# intermediate queued runs are skipped. Mirrors GitHub's official Pages workflow template.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: 10.0.x

      - name: Install WASM tools
        run: dotnet workload install wasm-tools

      - name: Publish Browser head
        run: dotnet publish Fluid.Avalonia.Acrylic.Demo.Browser/Fluid.Avalonia.Acrylic.Demo.Browser.csproj -c Release -o publish

      - name: Disable Jekyll (keep _framework, leading-underscore files)
        run: touch publish/wwwroot/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: publish/wwwroot

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
